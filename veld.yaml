x-veld:
  executable:
    about:
      description: "dummy veld for testing and showcasing"
    storage:
      path: /veld/storage
      format: "BaseX database"
      kind: "dummy data"

services:
  veld:
    build: .
    ports:
      - 8984:8984
    command: basexhttp
